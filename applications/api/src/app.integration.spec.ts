import { PrismaClient } from '@prisma/client';

describe('main.ts', () => {

  let prisma: PrismaClient;

  beforeAll(async () => {})

  afterAll(async () => {})

  describe('GET /tasks', () => {
    it('should return tasks', async () => {
    })
  });

  describe('POST /tasks', () => {});

  describe('PUT /tasks', () => {});
});
